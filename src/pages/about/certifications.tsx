import Head from "next/head";

const Home = () => {
  return (
    <div
      style={{ minHeight: "85vh", display: "flex", flexDirection: "column" }}
    >
      Certifications{" "}
    </div>
  );
};

export default Home;
